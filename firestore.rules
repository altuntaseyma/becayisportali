rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Tüm koleksiyonlara tam erişim ver (debug için)
    match /{document=**} {
      allow read, write: if true;
    }
  }
}